/**
 * Entry point kept at the repo root because index.html/index.tsx reference it.
 * The implementation lives in `app/`, split into providers, shell and router.
 */
export { default } from './app/App';
